
\PassOptionsToPackage{table}{xcolor}
\documentclass{article} % For LaTeX2e
\usepackage{iclr2026_conference,times}

\usepackage{hyperref}
\usepackage{url}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{xspace}
\usepackage{microtype}
\usepackage{subcaption}
\usepackage{multirow}
\usepackage{colortbl}

% Useful macros
\newcommand{\eg}{e.g.,\xspace}
\newcommand{\ie}{i.e.,\xspace}
\newcommand{\etal}{\textit{et al.}\xspace}
\newcommand{\mtrpose}{\textsc{MTR+Pose}\xspace}
\newcommand{\mtr}{\textsc{MTR}\xspace}
\newcommand{\minADE}{minADE\xspace}
\newcommand{\minFDE}{minFDE\xspace}

\definecolor{bestrow}{gray}{0.92}

\title{How Much Does Body Pose Help Pedestrian Trajectory\\Forecasting? A Seed-Replicated Study and\\Auxiliary-Loss Audit on Waymo}

% Authors must not appear in submitted version (double-blind).
\author{Anonymous Authors}

\newcommand{\fix}{\marginpar{FIX}}
\newcommand{\new}{\marginpar{NEW}}

%\iclrfinalcopy % Uncomment for camera-ready version.
\begin{document}

\maketitle

\begin{abstract}
Body pose is a plausible but rarely used signal for pedestrian trajectory
forecasting in autonomous driving. We extend the Motion Transformer (MTR) with a
GRU encoder over past SMPL body poses (6D rotation representation) and measure
what pose conditioning is actually worth on a pedestrian subset of the Waymo Open
Motion Dataset, using LiDAR-derived pose pseudo-ground-truth.
Our study has two parts. First, an \emph{auxiliary-loss audit}: an earlier draft
of this work credited geodesic SO(3) supervision with a 7.6\% \minADE improvement.
We show analytically and empirically that in our data condition the geodesic loss
receives \emph{identically zero gradient}---future pose targets are absent---so
the credited configurations differed only by random seed; the auxiliary signal that
actually trains the encoder is a winner-takes-all L1 pose-regression loss.
Second, a \emph{seed-replicated measurement} of the surviving questions, with
paired bootstrap confidence intervals over 6{,}688 validation pedestrians.
Whether pose helps depends on the encoder \emph{and} the context. With the
original GRU encoder, pose-conditioned training is heavy-tailed---8 replicates of
one configuration span \minADE 0.629--0.897---and on average \emph{worse} than
the no-pose baseline. Cross-attention over pose frames with sinusoidal positional
encoding is more stable and, at three seeds, appeared to give a $-3.5\%$
improvement; two further seeds reveal that this too was a small-sample artifact
($0.6549 \pm 0.0278$ at five seeds, one a heavy-tailed failure; not significant
under a seed-resampling bootstrap). In the noisy no-map condition, \emph{no} pose
configuration reliably beats the baseline. A single configuration does: with HD
map context (itself worth $-26\%$), the cross-attention+PE encoder gives $-1.6\%$,
stable across five seeds ($p{<}10^{-4}$)---the only seed-significant pose benefit we
find. It does not generalize: with the GRU encoder the map benefit is zero, and
after full-Waymo backbone pretraining (a further $-22\%$) the apparent pose benefit
does not survive five seeds. Even the surviving $-1.6\%$ is mostly explained by a
root-orientation channel the pose pipeline derives from past heading. Our results give a calibrated estimate of
pose's value---small, context-dependent, and partly a heading artifact---and a
cautionary, reproducible example (twice over, including our own correction) of how
few-seed ablations attribute gains to noise or inert components.
\end{abstract}

% ===========================================================================
\section{Introduction}
\label{sec:intro}
% ===========================================================================

Predicting where pedestrians will walk is a safety-critical problem in autonomous
driving. State-of-the-art trajectory forecasting models such as \mtr
\citep{shi2022mtr,shi2023mtrpp} achieve impressive accuracy by operating on
observed agent positions, velocities, and HD map geometry---but they discard a
potentially rich source of information: the \emph{body pose} of the pedestrian.

Human gait encodes intent. A pedestrian who is turning to face a crosswalk will
likely cross it; one whose torso is oriented away from traffic is unlikely to step
into the road. A runner's body orientation predicts turns before the feet complete
them. Modern perception systems increasingly provide body pose estimates
\citep{zhu2022motionbert}. While recent work uses skeleton keypoints for
pose-conditioned prediction in robot navigation and pedestrian benchmarks
\citep{salzmann2023robots,saadatnejad2024socialtransmotion,gao2025socialpose},
state-of-the-art autonomous driving forecasters operating on Waymo
\citep{ettinger2021waymo} discard body pose entirely.
This paper asks: \emph{does conditioning on past SMPL body poses improve
pedestrian trajectory prediction within a SOTA AV forecasting framework,
and if so, what design choices drive the gain?}

We extend \mtr with a \emph{pose conditioning branch} that encodes the past 11
timesteps of SMPL body pose \citep{loper2015smpl} using 6D rotation representations
\citep{zhou2019continuity}, a GRU encoder, and auxiliary future-pose prediction
heads. We train and evaluate on a pedestrian subset of the Waymo Open Motion
Dataset \citep{ettinger2021waymo} paired with LiDAR-derived SMPL pose
pseudo-ground-truth from Waymo-3DSkelMo \citep{zhu2025waymo3dskelmo},
using minADE and minFDE over 6 predicted modes.

This paper deliberately reports both a \emph{result} and a \emph{correction of our
own earlier result}, because the correction is instructive. An earlier single-run
draft credited geodesic SO(3) supervision with $-$7.6\% \minADE; an audit shows
that was impossible---in our data condition the future pose targets are zero, so
the geodesic loss is a constant with identically zero gradient and the two runs
compared were the same experiment under different seeds
(Section~\ref{sec:loss_ablation}). We rebuilt the empirical claims with three to
five seeds per configuration and paired bootstrap CIs over 6{,}688 validation
pedestrians. Our corrected findings:

\begin{itemize}
  \item \textbf{Without a strong backbone, no pose configuration reliably beats the
        baseline.} The GRU encoder of the earlier draft is heavy-tailed (8 replicates
        span $0.629$--$0.897$, worse than the baseline's $0.6604$ on average).
        Cross-attention with sinusoidal positional encoding looked like a stable
        $-3.5\%$ at three seeds ($\sigma\,0.0026$), but two more seeds---one a
        heavy-tailed failure---take it to $0.6549 \pm 0.0278$ and only $-0.8\%$ over
        baseline, not significant once seeds are resampled. Our own corrected headline
        was itself a small-sample artifact.
  \item \textbf{Temporal ordering carries what pose signal there is.} Adding a
        sinusoidal positional encoding to the cross-attention encoder improves it by
        $-4.5\%$ (the bag-of-frames variant $\approx$ baseline); auxiliary-loss
        composition is not a driver either---no active auxiliary beats the
        no-auxiliary control, and real future poses (99\% coverage) do not help.
  \item \textbf{Pose helps in exactly one configuration, by ${\sim}1.6\%$.} HD map
        alone is worth $-26\%$ and full-Waymo pretraining a further $-22\%$ (both
        trajectory-only). On top of the map, the cross-attention+PE encoder gives
        $-1.6\%$ (stable across five seeds, $p{<}10^{-4}$)---the only seed-significant
        pose benefit we find; the GRU gives nothing with map, and the apparent
        pretrained pose benefit does not survive five seeds. A control ablation
        attributes most of even the $-1.6\%$ to a root-orientation channel derived
        from past heading.
  \item \textbf{Few-seed ablations need seed replication.} The spread among replicates
        of one configuration ($0.27$ \minADE across 8 seeds' extremes) exceeds every
        architectural effect except map and pretraining; \emph{both} our earlier
        $-7.6\%$ and our first-corrected $-3.5\%$ headline were seed artifacts, caught
        only by replication. We describe the audit procedure so it transfers.
\end{itemize}

% ===========================================================================
\section{Related Work}
\label{sec:related}
% ===========================================================================

\subsection{Trajectory Forecasting for Autonomous Driving}

Modern trajectory prediction methods model multi-agent interactions over HD map
features using transformer architectures
\citep{vaswani2017attention,shi2022mtr,shi2023mtrpp,yuan2021agentformer,
salzmann2020trajectron}. Social GAN \citep{gupta2018socialgan} introduced
generative multimodal prediction for pedestrians; \mtr
\citep{shi2022mtr} advances this with global intention localization and
local refinement via cross-attention over map and agent context.
None of these methods incorporate body pose information.

\subsection{Human Pose Estimation and Pose Representations}

SMPL \citep{loper2015smpl} provides a parametric body model where each joint
rotation is represented as a 3D rotation matrix. For neural network learning,
\citet{zhou2019continuity} demonstrate that 6D rotation representations (the
first two columns of $SO(3)$) are continuous and better conditioned than
quaternions or axis-angle, particularly under gradient-based optimization.
The AMASS dataset \citep{mahmood2019amass} provides a large-scale archive of
motion-captured body animations in SMPL parameterization and is widely used to
train human motion priors \citep{rempe2021humor,he2022nemf}.
Waymo-3DSkelMo \citep{zhu2025waymo3dskelmo} combines LiDAR-based human mesh
recovery \citep{fan2023lidarhmr} with such priors to produce temporally coherent
SMPL pose sequences for Waymo pedestrians; we use its output as pose
pseudo-ground-truth.
Works such as MotionBERT \citep{zhu2022motionbert} and HumanMAC
\citep{chen2023humanmac} focus on pose quality but do not connect body pose to
downstream trajectory prediction. A separate line uses 2D pose for pedestrian
\emph{crossing-intention} classification \citep{fang2018pedestrian,rasouli2019pie},
supporting the premise that body configuration carries intent, though without
producing metric trajectory forecasts.

\subsection{Joint Pose and Trajectory Models}

Several recent works condition trajectory prediction on body pose.
\citet{salzmann2023robots} leverage 3D skeletal keypoints from RGB-D cameras
to improve trajectory prediction in robot navigation scenarios.
\citet{saadatnejad2024socialtransmotion} integrate 2D/3D skeleton pose cues
into a transformer-based predictor on pedestrian benchmarks, and
\citet{gao2025socialpose} extend this line to social navigation datasets
using skeleton-based body language features.
In contrast, our work uses parametric SMPL body pose in 6D rotation space---a
richer, anatomically-grounded representation---within the state-of-the-art MTR
framework on Waymo, and, with seed-replicated experiments and per-pedestrian
confidence intervals, isolates which design choices actually drive the gain:
auxiliary pose-loss design, temporal encoding (GRU vs.\ cross-attention $\pm$
positional encoding), future-pose supervision quality, and the interaction of
pose with HD map context and backbone pretraining.

% ===========================================================================
\section{Method}
\label{sec:method}
% ===========================================================================

\subsection{Background: Motion Transformer (MTR)}
\label{sec:mtr}

\mtr \citep{shi2022mtr} is a transformer-based trajectory predictor for the
Waymo Open Motion Dataset. For each \emph{center object} (the agent being predicted),
it encodes a scene context tensor from agent histories and HD map polylines via
a PointNet-like encoder, then decodes $K=6$ trajectory modes through
iterative cross-attention over the scene context, guided by $K$ learned
intention queries that are globally localized to trajectory clusters.

The loss has three components: a mode classification loss $\mathcal{L}_{\text{cls}}$,
a winner-takes-all regression loss $\mathcal{L}_{\text{reg}}$, and a velocity
consistency loss $\mathcal{L}_{\text{vel}}$.

\subsection{Pose Conditioning Branch}
\label{sec:pose_branch}

We add a pose conditioning branch alongside the trajectory decoder, illustrated
in Figure~\ref{fig:architecture}. The branch has four components:

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{fig_architecture.pdf}
\caption{%
  \textbf{MTR+Pose architecture.}
  A GRU pose encoder processes the past 11 SMPL body poses (each represented
  as a 144D 6D-rotation vector) and produces a 256D hidden state that is
  concatenated with the MTR agent feature and projected before the decoder.
  Auxiliary future-pose prediction heads are trained with a winner-takes-all
  L1 loss $\mathcal{L}_{\text{wta}}$ on 6D rotations; gradients from this
  auxiliary task are the only path by which pose supervision shapes the
  shared features.
  The trajectory decoder and map attention modules of MTR are unchanged.
}
\label{fig:architecture}
\end{figure}

\paragraph{6D Rotation Encoding.}
Each SMPL body pose at timestep $t$ consists of 24 joint rotations. We represent
each rotation as a 6D vector \citep{zhou2019continuity} (the first two columns of
the $3{\times}3$ rotation matrix), giving a 144D pose vector per timestep.

\paragraph{GRU Pose Encoder.}
Given the past $T=11$ pose vectors $\mathbf{p}_1, \ldots, \mathbf{p}_{T} \in \mathbb{R}^{144}$,
a two-layer GRU with hidden size 256 processes the sequence:
\begin{equation}
  \mathbf{h}_t = \text{GRU}(\mathbf{p}_t, \mathbf{h}_{t-1}), \quad \mathbf{h}_T \in \mathbb{R}^{256}.
\end{equation}
The final hidden state $\mathbf{h}_T$ is concatenated with the MTR agent feature
and projected to 256D via an MLP, forming the fused agent representation.

\paragraph{Pose Prediction Heads.}
Each of the decoder layers additionally predicts $K$ modes of future poses:
at layer $\ell$ and mode $k$, a linear head outputs 6D rotations for all 80
future timesteps (a $80{\times}144$ output). These heads are auxiliary---their
predictions never feed back into the trajectory pathway; their only effect on
trajectory prediction is through the gradients their losses send into the shared
decoder features and the GRU encoder.

\paragraph{Auxiliary Pose Losses.}
Let $\hat{\mathbf{r}}^{ij} \in \mathbb{R}^6$ be the predicted 6D rotation and
$\mathbf{r}^{ij} \in \mathbb{R}^6$ be the ground-truth 6D rotation for joint $j$
at future step $i$. We consider four auxiliary signals:

\begin{itemize}
  \item \textbf{Winner-takes-all L1 (WTA-L1)}: $\mathcal{L}_{\text{wta}}$ selects,
        per pedestrian, the pose mode closest to the ground truth in 6D L2 distance,
        and applies an L1 regression loss to that mode, masked by per-timestep
        trajectory validity. (In our configuration files this loss is named
        \texttt{gmm\_pose} for historical reasons; it is a plain WTA regression,
        not a Gaussian mixture likelihood.)

  \item \textbf{Geodesic loss}: $\mathcal{L}_{\text{geo}} = \frac{1}{IJ}\sum_{i,j}
        \arccos\!\left(\text{clamp}\!\left(\frac{\text{tr}(\mathbf{R}_i^j{}^\top \hat{\mathbf{R}}_i^j) - 1}{2},\,-1{+}\epsilon,\,1{-}\epsilon\right)\right)$,
        where $\mathbf{R}, \hat{\mathbf{R}} \in SO(3)$ are rotation matrices
        recovered from the 6D representations of the WTA-selected mode, averaged
        over all future steps (unmasked). The clamp ($\epsilon{=}10^{-7}$) avoids the
        divergent $\arccos$ derivative at the $\pm1$ boundary.

  \item \textbf{MPJPE}: $\mathcal{L}_{\text{mpjpe}} = \frac{1}{IJ}\sum_{i,j}
        \|\mathbf{x}_i^j - \hat{\mathbf{x}}_i^j\|_1$, where $\mathbf{x}$ are joint
        positions computed via forward kinematics on SMPL.

  \item \textbf{Pose mode classification}: $\mathcal{L}_{\text{cls\_pose}}$,
        a cross-entropy loss on per-mode pose scores against the WTA-selected mode.
\end{itemize}

The total loss adds the three MTR motion terms ($\mathcal{L}_{\text{cls}} +
\mathcal{L}_{\text{reg}} + 0.5\,\mathcal{L}_{\text{vel}}$) to the weighted pose
terms $w_{\text{wta}}\mathcal{L}_{\text{wta}} + w_{\text{geo}}\mathcal{L}_{\text{geo}}
+ w_{\text{mpjpe}}\mathcal{L}_{\text{mpjpe}} + w_{\text{cls\_pose}}\mathcal{L}_{\text{cls\_pose}}$;
we ablate $w_\star \in \{0, 0.05, 0.1, 0.2\}$ (weights of $1.0$ diverge to NaN
through the SMPL forward pass, so all stable runs use $w \leq 0.2$).

\paragraph{A caveat discovered post-hoc: future pose ground truth is absent in the main data condition.}
\label{par:inert}
In our primary (10\,fps) data condition, SMPL pose observations cover only the
\emph{past} window ($t \leq 1.0$\,s); future pose targets are zero for 99.99\% of
timesteps (5 of 91{,}520 in the training split). As we show analytically and
empirically in Section~\ref{sec:loss_ablation}, this renders the geodesic term
\emph{inert} (identically zero gradient against all-zero targets, which map to the
all-zero matrix under Gram--Schmidt), while WTA-L1, MPJPE, and
$\mathcal{L}_{\text{cls\_pose}}$ stay active but pull predictions toward the
degenerate zero targets. We report it prominently because it reverses a conclusion
an earlier draft drew from single-run comparisons.

\subsection{Cross-Attention Pose Encoders (Ablation)}
\label{sec:cross_attn}

To isolate the contribution of temporal ordering from sequential integration, we
test two cross-attention pose encoder variants. Let $\mathbf{q} \in \mathbb{R}^{256}$
be the MTR agent feature (projected query) and $\mathbf{K}, \mathbf{V} \in \mathbb{R}^{11 \times 256}$
be the 11 projected pose frames (keys and values). The cross-attention output is:
\begin{equation}
  \mathbf{c} = \operatorname{softmax}\!\left(\frac{\mathbf{q}\mathbf{K}^\top}{\sqrt{256}}\right)\mathbf{V},
\end{equation}
and $\mathbf{c}$ is concatenated with $\mathbf{q}$ and projected to 256D, replacing $\mathbf{h}_T$ in the fusion step.

\textbf{Cross-attention without positional encoding.} No temporal information is injected before attention:
$\mathbf{K} = \{W_K \mathbf{p}_t\}_{t=1}^{11}$, $\mathbf{V} = \{W_V \mathbf{p}_t\}_{t=1}^{11}$.
This treats pose frames as a permutation-invariant set and tests whether the
raw content of each pose frame---absent any ordering---is sufficient to condition
trajectory prediction.

\textbf{Cross-attention with sinusoidal positional encoding.} Standard sinusoidal positional encoding
\citep{vaswani2017attention} is added to the projected keys and values before attention:
$\mathbf{K}' = \{W_K \mathbf{p}_t + \mathbf{e}_t\}_{t=1}^{11}$, where $\mathbf{e}_t$ is
the fixed sinusoidal embedding for frame index $t \in \{0, \ldots, 10\}$.
This injects temporal ordering at zero extra parameter cost and tests whether
ordering alone is sufficient, without the causal accumulation structure of a GRU.

% ===========================================================================
\section{Experiments}
\label{sec:experiments}
% ===========================================================================

\subsection{Dataset and Setup}
\label{sec:setup}

\paragraph{Dataset.}
We use a pedestrian-only subset of the Waymo Open Motion Dataset
\citep{ettinger2021waymo} paired with SMPL body pose pseudo-ground-truth from the
Waymo-3DSkelMo pipeline \citep{zhu2025waymo3dskelmo}. The subset contains 579
training scenes (1{,}348 pedestrians, of which 1{,}144 have sufficient valid past
observations to serve as prediction targets) and 5{,}171 validation scenes
(8{,}282 pedestrians). Ground-truth future trajectories come from Waymo tracking
data (91 timesteps at 10\,Hz; 9.0\,s total span --- 1.0\,s past plus an 8.0\,s
future horizon). 6{,}688 of the 8{,}282 validation
pedestrians (80.8\%) have valid future trajectory data and enter the evaluation.

\paragraph{Pose provenance.}
The SMPL poses are not motion-capture ground truth: they are estimated from Waymo
LiDAR by the Waymo-3DSkelMo pipeline \citep{zhu2025waymo3dskelmo}, which fits
per-frame SMPL bodies with LiDAR-HMR \citep{fan2023lidarhmr} and refines them with
AMASS-trained motion priors \citep{mahmood2019amass,rempe2021humor,he2022nemf}. Two
properties matter for our results. \emph{First}, pose observations exist only for the
past window in our main condition, so no future information leaks into the inputs; the
evaluation is sound. \emph{Second}, the pipeline snaps the SMPL root rotation to the
past-trajectory motion direction whenever the pedestrian is moving and the raw LiDAR
estimate deviates by more than $90^{\circ}$, so the root-orientation channel is
partially derived from position history the model already receives---an overlap we
quantify in Section~\ref{sec:temporal_ablation}. Body configuration and gait within
$\pm 90^{\circ}$ are genuinely perception-derived.

\paragraph{Evaluation.}
We report \textbf{\minADE} (minimum Average Displacement Error over 6 modes, in meters)
and \textbf{\minFDE} (minimum Final Displacement Error). Lower is better.
The evaluation covers the full 8.0\,s future horizon (80 steps at 10\,Hz).

\paragraph{Training and statistical protocol.}
All models train for 30 epochs with batch size 10 on a single RTX 4090
(AdamW, LR $10^{-4}$, step decay $\times 0.5$ at epochs 10/20/25, gradient-norm
clip 1.0); a full run takes 10--15 minutes. Validation is run at epochs
1, 2, 4, every second epoch up to 20, and every epoch from 21 to 30.
Following common practice, our per-run headline number is the \emph{best}
validation \minADE over these evaluations; because best-of-$n$ selection on a
noisy curve inflates apparent differences, every key comparison is additionally
run with \textbf{three to five random seeds} (five for the headline, encoder, map,
and pretrain cells; three for secondary cells), and we report
mean~$\pm$~std across seeds
together with \textbf{paired bootstrap} 95\% confidence intervals over the
6{,}688 evaluated pedestrians (per-pedestrian errors averaged across seeds,
resampled jointly for both models in a pair; 10{,}000 resamples).

\paragraph{Scale caveat.}
The 579-scene training subset is a small fraction of the full Waymo training set
(${\approx}487$k scenarios), so absolute \minADE values in
Sections~\ref{sec:main_result}--\ref{sec:map_ablation} are far from
full-data state of the art; relative, seed-replicated comparisons between models
trained on identical data are the object of study.
Section~\ref{sec:pretrain_ablation} quantifies the scale effect directly.

\subsection{Main Result: Pose Conditioning vs.\ Baseline}
\label{sec:main_result}

Table~\ref{tab:main} compares, over five seeds each, the trajectory-only
\mtr baseline against three pose-conditioned variants: the GRU encoder with the
WTA-L1 auxiliary pose loss ($w{=}0.1$; the configuration an earlier draft
mislabeled ``geodesic-only'', see Section~\ref{sec:loss_ablation}), the GRU
encoder with \emph{no} active auxiliary pose loss, and the cross-attention
encoder with sinusoidal positional encoding (same WTA-L1 auxiliary loss).

\begin{table}[t]
\caption{Main result (no map; best-checkpoint protocol; 5 seeds each). Per-seed
values expose a heavy-tailed failure mode in \emph{both} GRU- and attention-based
pose conditioning (bold = degraded seeds) that mean$\pm$std alone understates. No
row is significantly below the baseline under a seed-resampling bootstrap.}
\label{tab:main}
\begin{center}
\small
\begin{tabular}{llcc}
\toprule
Model & Aux.\ loss & \minADE $\downarrow$ (mean$\pm$std) & per-seed values \\
\midrule
\mtr (no pose)          & ---         & $0.6604 \pm 0.0143$ & .655 .683 .664 .654 .646 \\
\mtrpose GRU, no aux    & none active & $0.6545 \pm 0.0271$ & .696 .665 .639 .626 .647 \\
\mtrpose GRU, WTA-L1    & $w{=}0.1$   & $0.6853 \pm 0.0651$ & .639 \textbf{.785} .629 \textbf{.717} .657 \\
\mtrpose XAttn+PE, WTA-L1 & $w{=}0.1$ & $0.6549 \pm 0.0278$ & .640 .634 .637 .663 \textbf{.700} \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

The seed-replicated picture inverts the earlier draft's headline, and cuts deeper
than our first correction did. The GRU encoder with the WTA-L1 auxiliary
loss---credited with $-7.6\%$ from one lucky run---is \emph{worse than baseline on
average}: two of five seeds converge to degraded optima (bold), and the pedestrian
bootstrap puts it at $+3.8\%$ \minADE. The cross-attention encoder with positional
encoding \emph{appeared} to deliver what the draft attributed to the GRU: at three
seeds it was a remarkably stable $-3.5\%$ ($\sigma\,0.0026$). Two additional seeds
(404, 505) puncture this---seed 505 is a heavy-tailed failure (best-epoch $0.700$,
final-epoch \minADE $1.0$), and the five-seed cell is $0.6549 \pm 0.0278$: a tenfold
increase in spread and only $-0.8\%$ over baseline, \emph{not} significant under a
seed-resampling bootstrap ($p{=}0.70$). The attention encoder is thus heavy-tailed
too; its three-seed stability was itself a small-sample artifact---the very failure
mode this paper documents, now reproduced in our own corrected result. Under seed
resampling, \emph{no} no-map configuration---GRU or attention, with or without an
auxiliary loss---reliably beats the no-pose baseline (the no-auxiliary GRU control
is $-0.9\%$ pedestrian-bootstrap but $p{=}0.59$ hierarchical). Pose's benefit, if
any, must be sought where the trajectory backbone is already
strong (Sections~\ref{sec:map_ablation}--\ref{sec:pretrain_ablation}).

\subsection{The Auxiliary-Loss Audit: an Inert Term Credited with the Headline Gain}
\label{sec:loss_ablation}

An earlier draft ablated the pose supervision with one run per configuration and
concluded geodesic-only supervision was the critical ingredient ($0.6231$,
$-7.6\%$ vs.\ baseline $0.6745$), beating ``WTA-L1 only'' ($0.6467$). Three facts
reverse that conclusion.

\paragraph{Fact 1: the geodesic term received zero gradient.}
In the 10\,fps data condition, future pose targets are zero for all but 5 of
91{,}520 training steps (Appendix~\ref{app:dataset}). A zero 6D target maps under
Gram--Schmidt to the all-zero matrix $\mathbf{R}_{\text{gt}}{=}\mathbf{0}$, for
which $\text{tr}(\hat{\mathbf{R}}^\top \mathbf{R}_{\text{gt}}) \equiv 0$ for
\emph{any} prediction, so $\mathcal{L}_{\text{geo}} \equiv \arccos(-\tfrac12)$, a
constant; we verified its gradient is exactly zero (and nonzero under random targets).

\paragraph{Fact 2: the two compared configurations were therefore identical.}
The ``geodesic-only'' run also carried WTA-L1 at $w{=}0.1$, so with the geodesic
term inert it ran the same training distribution as ``WTA-L1 only''---their
$0.0236$ gap ($0.6231$ vs.\ $0.6467$) is pure run-to-run noise, larger than most
effects the draft reported.

\paragraph{Fact 3: seed replication measures the noise directly.}
Re-run with multiple seeds (Table~\ref{tab:loss_ablation}), the pooled
\textbf{eight} replicates of this single configuration span \minADE
$0.629$--$0.897$ ($0.7060 \pm 0.0921$); both draft values sit comfortably inside
this one-configuration spread, confirming the ``geodesic effect'' was a draw from it.

\begin{table}[t]
\caption{Pose supervision ablation, seed-replicated (no map, GRU encoder,
$w{=}0.1$ for active losses). ``No aux'' has no active auxiliary pose loss (the
inert geodesic term only), so the pose encoder is trained by trajectory gradients
alone. The WTA-L1 row pools the draft's ``geodesic-only'' and ``WTA-L1 only''
cells, which are functionally identical. Single-run draft values shown for
comparison.}
\label{tab:loss_ablation}
\begin{center}
\footnotesize
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{lccc}
\toprule
Aux.\ pose loss & \minADE (mean$\pm$std) & per-seed values & single-run (draft) \\
\midrule
None (no pose)        & $0.6604 \pm 0.0143$ & .655 .683 .664 .654 .646 & 0.6745 \\
No aux (inert geo)    & $0.6545 \pm 0.0271$ & .696 .665 .639 .626 .647 & --- \\
WTA-L1 (8 seeds, pooled) & $0.7060 \pm 0.0921$ & .639 .785 .629 .717 .657 .897 .662 .662 & 0.6467 / 0.6231$^{\dagger}$ \\
MPJPE + WTA-L1        & $0.6769 \pm 0.0421$ & .724 .662 .644 & 0.6576 \\
All (MPJPE+WTA+cls)   & $0.6574 \pm 0.0237$ & .668 .674 .630 & 0.6532 \\
\bottomrule
\end{tabular}
\end{center}
{\footnotesize $^{\dagger}$The earlier draft reported these as two different
configurations (``WTA-L1 only'' and ``geodesic-only''); they are the same
configuration, and their spread is seed noise.}
\end{table}

The corrected interpretation reverses the draft on a second point as well: far
from being ``the critical ingredient'', the auxiliary losses are---with the GRU
encoder and degenerate zero-valued targets---associated with \emph{instability}.
Four of eleven runs across the WTA-L1 and MPJPE+WTA-L1 cells converged badly
($\geq 0.71$), whereas the no-auxiliary control produced no such seed in five
runs. At our statistical power the active-loss variants cannot be usefully ranked
against one another, and the earlier draft's mechanistic story about geodesic
gradients shaping the GRU was fitted to noise.

\paragraph{The mask fix removes the instability, but adds no signal.} Because the
audit shows the pose losses train against absent targets, we re-ran the four audit
cells with a corrected future-pose validity mask (losses averaged only over the
$<{0.01\%}$ of steps that carry a real target; this is the released code). Masking
removes the catastrophic tail---the WTA-L1 cell's worst seed falls from $0.897$ to
$0.679$ ($\sigma\; 0.065{\to}0.011$)---confirming the instability came from training
toward degenerate zero targets. But it does not turn the loss into a benefit: the
masked WTA-L1 cell ($0.6715 \pm 0.0112$) is still $+5.5\%$ worse than the masked
no-auxiliary control ($0.6365$; hierarchical $p{<}10^{-4}$), and the fix leaves the
no-pose baseline unchanged ($0.6602 \pm 0.0149$ masked vs.\ $0.6604$; $p{=}0.95$).
The on-10fps auxiliary pose signal is, as predicted, inert.

\paragraph{Reproducibility note.} Table~\ref{tab:loss_ablation} and the GRU/attention
heavy tails were produced with the \emph{pre-fix} code (tagged in the release); the
released code masks the future-pose losses, so re-running these cells reproduces the
masked numbers above rather than the table. The encoder, map, and pretraining results
are mask-insensitive (past poses are non-zero on 10fps) and reproduce either way.

\subsection{Ablation: Temporal Encoding --- GRU vs.\ Cross-Attention}
\label{sec:temporal_ablation}

Table~\ref{tab:temporal} compares three temporal encoding strategies (three to
five seeds each), all with the WTA-L1 auxiliary loss at $w{=}0.1$.

\begin{table}[t]
\caption{Temporal encoding ablation, seed-replicated (no map; WTA-L1 auxiliary
loss at $w{=}0.1$ in all pose-conditioned variants).}
\label{tab:temporal}
\begin{center}
\small
\begin{tabular}{lccc}
\toprule
Pose Encoder & Temporal info & \minADE (mean$\pm$std) & per-seed values \\
\midrule
None (no pose)               & ---               & $0.6604 \pm 0.0143$ & .655 .683 .664 .654 .646 \\
Cross-attention, no PE       & none (bag)        & $0.6667 \pm 0.0407$ & .714 .641 .646 \\
Cross-attention + sinus.\ PE & ordering only     & $0.6549 \pm 0.0278$ & .640 .634 .637 .663 .700 \\
GRU (sequential)             & ordering + causal & $0.6853 \pm 0.0651$ & .639 .785 .629 .717 .657 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\emph{Ordering is necessary}: the bag-of-frames encoder is slightly worse than the
no-pose baseline ($+1.0\%$)---unordered pose content contributes nothing---while
adding only a fixed sinusoidal positional encoding improves the same architecture by
$-4.5\%$ over the no-PE variant (CI $[-5.2, -3.7]$, $p{=}0.008$ hierarchical over the
three shared seeds). Positional encoding thus genuinely helps the cross-attention
encoder. What it does \emph{not} do is lift that encoder clearly above using no pose
at all: as Section~\ref{sec:main_result} shows, at five seeds xattn+PE is heavy-tailed
and only $-0.8\%$ over baseline (n.s.). The GRU is worse still (its $0.629$--$0.897$
spread is wider than the attention encoder's), so the draft's ``GRU beats attention''
ranking does reverse---but neither encoder is a stable improvement over the no-pose
baseline in this no-map regime.

\paragraph{Root orientation carries what gain there is.} A control ablation exposes
\emph{what} in the pose the encoder uses. Zeroing the root-orientation channel of the
xattn+PE input (joint 0; body joints untouched) raises \minADE to $0.6633 \pm 0.0031$
---statistically \emph{indistinguishable from the no-pose baseline} ($0.6604$;
hierarchical $p{=}0.51$) and $+4.1\%$ worse than the (three-seed) xattn+PE model
(hierarchical CI $[+3.1, +5.3]\%$, $p{<}10^{-4}$). So even in the seeds where the
encoder does well, its advantage rides entirely on the root-orientation channel,
which the Waymo-3DSkelMo pipeline partially derives from past-trajectory heading
(Section~\ref{sec:setup}). The usable pose signal is essentially heading re-entering
through a side channel, not body configuration.

\subsection{Ablation: HD Map Context}
\label{sec:map_ablation}

The experiments above use zero-padded map features (the SMPL-paired pedestrian
subset was originally processed without HD map polylines). To quantify how map
context interacts with pose conditioning, we enable HD map loading and train the
2$\times$2 factorial design: $\pm$map $\times$ $\pm$pose (GRU encoder, WTA-L1
auxiliary loss at $w{=}0.1$), three seeds per cell.

\begin{table}[t]
\caption{Map $\times$ pose ablation, seed-replicated (5 seeds per cell; the GRU+map
cell 3). WTA-L1 auxiliary loss at $w{=}0.1$ in pose cells. With map context the seed
variance collapses by an order of magnitude relative to the no-map cells.}
\label{tab:map_ablation}
\begin{center}
\small
\begin{tabular}{llcc}
\toprule
Pose Enc. & Map & \minADE (mean$\pm$std) & per-seed values \\
\midrule
None      & No map   & $0.6604 \pm 0.0143$ & .655 .683 .664 .654 .646 \\
None      & With map & $0.4854 \pm 0.0047$ & .490 .484 .489 .478 .486 \\
GRU       & No map   & $0.6853 \pm 0.0651$ & .639 .785 .629 .717 .657 \\
GRU       & With map & $0.4854 \pm 0.0057$ & .480 .491 .485 \\
\rowcolor{bestrow}
XAttn+PE  & With map & $\mathbf{0.4778 \pm 0.0041}$ & .477 .481 .480 .471 .480 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

Table~\ref{tab:map_ablation} reveals three findings.
\emph{First}, HD map is the dominant signal: it alone reduces \minADE by $26.5\%$
(CI $[-29.0, -24.1]$), far more than any pose effect, since routing is encoded
directly in the map.
\emph{Second}, this is where pose's one seed-significant benefit lives---and it
requires the right encoder. The GRU cell is \emph{identical} to no-pose+map at five
seeds ($-0.0\%$, $p{=}0.99$), but the xattn+PE encoder gives $-1.57\%$ (hierarchical
CI $[-2.4, -0.8]\%$, $p{<}10^{-4}$, five seeds both arms). Unlike the no-map xattn+PE
cell, this one is \emph{stable} ($\sigma\,0.004$): the map both supplies context and
tames the encoder's heavy tail. The benefit is still small---partial redundancy
between body orientation and the map's routing constraints, and the
root-orientation channel's derivation from past heading
(Sections~\ref{sec:temporal_ablation},~\ref{sec:setup}) accounts for most of it.
\emph{Third}, map context stabilizes training generally: per-seed spread collapses
($\sigma \leq 0.006$ vs.\ up to $0.135$ without map; Table~\ref{tab:multiseed}), and
the failure mode of Section~\ref{sec:main_result} never occurs in a map-enabled seed.

\subsection{Ablation: Backbone Pretraining on Full Waymo}
\label{sec:pretrain_ablation}

The 579-scene subset used above is ${\approx}0.12\%$ of the full Waymo training set
(${\approx}487$k scenarios; $486{,}995$ exactly). To test whether dataset scale is the binding constraint
on absolute accuracy, and to isolate pose's contribution when the backbone already
encodes a strong trajectory prior, we run a final 2$\times$2 ablation: $\pm$pretrain
$\times$ $\pm$pose, with HD map enabled in all four cells.

We pretrain the trajectory-only backbone (no pose encoder) on the full Waymo pedestrian
set---231{,}507 per-agent training examples drawn from 486{,}995 scenarios---for 30 epochs
with the same hyperparameters as before. To avoid corrupting this backbone at fine-tune
iter 0, we use a \emph{residual zero-init} pose fuser: its output is \emph{added} to the
backbone feature and its final linear layer is zero-initialized, so the pose pathway
contributes exactly zero initially (the backbone passes through unchanged) and warms up
additively.

\begin{table}[t]
\caption{Pretrain $\times$ pose ablation, seed-replicated (three seeds per cell).
All runs include HD map and use 30-epoch fine-tuning on the 579-scene subset
(the no-pretrain rows repeat Table~\ref{tab:map_ablation}'s map cells).
Pretrained variants load the same full-Waymo backbone checkpoint via residual
zero-init pose fusion; fine-tuning seeds vary.}
\label{tab:pretrain_ablation}
\begin{center}
\small
\begin{tabular}{llccc}
\toprule
Pretrain & Pose Enc. & \minADE (mean$\pm$std) & \minFDE (mean$\pm$std) & per-seed \minADE \\
\midrule
None  & ---  & $0.4854 \pm 0.0047$ & $1.0493 \pm 0.0150$ & .490 .484 .489 .478 .486 \\
None  & GRU  & $0.4854 \pm 0.0057$ & $1.0362 \pm 0.0060$ & .480 .491 .485 \\
Full Waymo & ---  & $0.3745 \pm 0.0071$ & $0.7915 \pm 0.0159$ & .382 .375 .380 .371 .364 \\
Full Waymo & GRU  & $0.3718 \pm 0.0059$ & $0.7809 \pm 0.0097$ & .374 .376 .365 \\
Full Waymo & XAttn+PE & $0.3718 \pm 0.0034$ & $0.7890 \pm 0.0111$ & .371 .371 .378 .369 .370 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

Pretraining is the second dominant lever and is robust: it reduces \minADE by
$22.9\%$ without pose and $23.4\%$ with pose ($p{<}10^{-4}$)---by far the largest
effect after map, and, like map, trajectory-only. The pose contribution \emph{on
top}, however, does not survive five seeds. At three seeds it looked like $-1.9\%$;
extending the no-pose fine-tune to five seeds (its two extra seeds are among its
best) shrinks the gap to $-0.7\%$ for both encoders---hierarchical bootstrap
$p{=}0.35$ (xattn+PE) and $p{=}0.09$ (GRU), neither significant. The two encoders are
themselves indistinguishable once pretrained ($+0.4\%$, $p{=}0.77$), so the
from-scratch encoder distinction vanishes against a strong prior. The residual
zero-init fuser keeps every pretrained cell stable (no failure seeds), but
pretraining, like the no-map condition, does not establish a pose benefit---only the
map\,$\times$\,xattn+PE cell does. Qualitative examples are in
Appendix~\ref{app:qualitative}, Figure~\ref{fig:qualitative}.

% ===========================================================================
\section{Analysis and Insights}
\label{sec:analysis}
% ===========================================================================

\paragraph{How an inert loss became a headline result.}
The failure chain is worth spelling out because none of its links is exotic: a data
condition silently degenerated (all-zero future-pose targets), the loss evaluated to
a smooth constant with \emph{zero} gradient rather than crashing, two functionally
identical configurations were each run once, and the resulting $0.024$ seed gap was
read as a $3.8\%$ design effect and given a mechanistic story. The audit that caught
it generalizes to two cheap checks: verify the \emph{target coverage} of every
auxiliary loss against the data, and check the \emph{gradient} of every loss term in
isolation on a real batch.

\paragraph{What little the pose branch does, and how.}
Where pose helps at all (the map cell), the signal is \emph{temporal} and comes
from \emph{encoding}, not supervision. Unordered pose content is useless
(bag-of-frames $\approx$ baseline), and attention extracts the ordered signal more
reliably than the GRU (Section~\ref{sec:temporal_ablation}). Supervision adds
nothing: real dense future-pose targets---the only condition in which the
rotation-space losses carry gradient---do not move accuracy
(Appendix~\ref{sec:future_ablation}), and the auxiliary WTA-L1 loss is not required
(the no-auxiliary control matches or edges the active-loss cells, though it too is
not seed-significant). Its
degenerate zero targets are plausibly counterproductive, anchoring shared features
toward a constant, which may explain why the $20\%$ heavy-tailed failure rate of
no-map pose runs (bad basins, not late divergence) concentrates in auxiliary-active
GRU cells and vanishes with map context or pretraining---reinforcing that on small
benchmarks per-seed values or failure rates must be reported, not seed means alone.

% ===========================================================================
\section{Limitations}
\label{sec:limitations}
% ===========================================================================

\textbf{Dataset scale, and why we cannot go bigger.} Our ablations use a 579-scene
subset, and SMPL pose pseudo-ground-truth exists \emph{only} for this subset---the
full-Waymo backbone we pretrain is necessarily trajectory-only. Pose-conditioning at
full scale is therefore not just uncomputed but infeasible with current annotations.
Within reach, pretraining does not rescue pose: the pretrained pose benefit that
looked like $-1.9\%$ at three seeds fell to $-0.7\%$ (n.s.) once the no-pose
fine-tune was measured at five seeds. Whether denser or cleaner pose labels at scale
would change the picture is untestable here.

\textbf{Pose provenance and decoder.} The LiDAR-derived SMPL pseudo-ground-truth
(Waymo-3DSkelMo) is matched to Waymo tracks; its root-orientation correction
partially couples the root channel to past trajectory (Section~\ref{sec:setup}), so
part of the pose signal overlaps with observed position history---indeed, zeroing
that channel removes the entire xattn+PE gain (Section~\ref{sec:temporal_ablation}),
so much of the benefit is heading re-entering, not new body information. Online pose
estimation in deployment would also carry different noise, and we do not evaluate
predicted-pose quality independently (our metric is trajectory only).

\textbf{Statistical power and two bootstrap levels.} Our primary paired bootstrap
resamples pedestrians on \emph{seed-averaged} errors; we also report a
\emph{hierarchical} bootstrap resampling seeds then pedestrians---the conservative
test we rely on for significance. Under it the map ($-26\%$) and pretraining
($-22\%$) trajectory effects survive strongly ($p{<}10^{-4}$), and exactly one pose
effect does---map\,$\times$\,xattn+PE, $-1.6\%$, $p{<}10^{-4}$---while every other
pose margin does not (no-map xattn+PE $p{=}0.70$, GRU+map $p{=}0.99$, pretrained pose
$p{=}0.35$). The pedestrian-only bootstrap would have called several of these
significant; requiring seed resampling is what caught two of our own headline claims.
The best-checkpoint protocol (kept for comparability) selects on validation noise;
all CIs cover one validation split only.

% ===========================================================================
\section{Conclusion}
\label{sec:conclusion}
% ===========================================================================

We have presented \mtrpose, a pose-conditioned extension of the Motion Transformer
that incorporates past SMPL body poses via a GRU encoder with 6D rotation
representations, together with a seed-replicated measurement of what pose
conditioning is worth and an audit that retracts our own earlier headline claim.
Our conclusions: (1) the previously credited geodesic SO(3) supervision was
inert---zero gradient against absent future-pose targets---so its $7.6\%$ was
seed noise between two identical configurations. (2) In the no-map condition
\emph{no} configuration reliably beats the no-pose baseline: cross-attention+PE
looked like a stable $-3.5\%$ at three seeds but is heavy-tailed at five
($-0.8\%$, n.s.)---a second small-sample artifact, in a paper about small-sample
artifacts. (3) Pose helps in exactly one configuration: the
cross-attention+PE encoder \emph{with} HD map, by $-1.6\%$ (stable across five
seeds, $p{<}10^{-4}$). The GRU gives nothing with map, and the apparent pretrained
pose benefit does not survive five seeds; a control ablation attributes most of even
the surviving $-1.6\%$ to a root-orientation channel derived from past heading. (4) The mechanism is the past-pose encoder, not future-pose
prediction. The audit and seed-replication methodology---which caught two of our
own headline claims---not the pose numbers, is our intended lasting contribution.

\subsubsection*{Reproducibility Statement}
All experiments use the publicly available Waymo Open Motion Dataset and the
open-source Waymo-3DSkelMo pose-extraction pipeline; neither is redistributed by
us. Training configurations (YAML), the multi-seed launch script, the per-pedestrian
metric logger, and the bootstrap analysis script are included in our code release,
together with the exact random seeds (101/202/303, plus 404/505 for the headline,
encoder, map, and pretrain cells) and the per-epoch validation
protocol described in Section~\ref{sec:setup}. Appendix~\ref{app:details} lists
all hyperparameters; Appendix~\ref{app:dataset} documents dataset construction,
including the timestamp-alignment and future-pose-coverage details on which the
inert-gradient analysis rests. We will release the code upon publication.

\subsubsection*{Acknowledgments}
The authors gratefully acknowledge access to GPU compute (RTX 4090) and the
publicly available Waymo Open Motion Dataset.

\subsubsection*{LLM Disclosure}
The authors used large language model (LLM) assistance (Claude, Anthropic) during
the preparation of this manuscript for writing drafts, code assistance, and
literature search support. All experimental results, scientific analysis, and
conclusions are the authors' own.

\bibliography{references}
\bibliographystyle{iclr2026_conference}

% ===========================================================================
\appendix
\section{Additional Experimental Details}
\label{app:details}
% ===========================================================================

\subsection{Architecture Hyperparameters}
\begin{itemize}
  \item GRU pose encoder: GRU(input=144, hidden=256, layers=2, dropout=0.1);
        ${\approx}505$K parameters total including fusion MLP
  \item Cross-attention pose encoder: pose projection 144$\to$256,
        MultiheadAttention(embed\_dim=256, num\_heads=8, dropout=0.1,
        batch\_first=True); ${\approx}497$K parameters total including fusion MLP
  \item Pose input dimension: 144 (24 joints $\times$ 6D rotation)
  \item Past pose window: 11 timesteps (1.0 second at 10\,Hz)
  \item Number of predicted trajectory modes: $K=6$
  \item Number of future steps: 80 (8.0 seconds)
  \item MTR context encoder: 6 local-attention transformer layers, 256 hidden dim,
        8 heads, attention dropout 0.1
  \item MTR decoder: 6 layers, hidden dim 512 (map branch 256), 8 heads
  \item Batch size: 10
  \item Optimizer: AdamW, $\beta_1=0.9$, $\beta_2=0.999$, weight decay 0.01
  \item LR schedule: initial LR $1{\times}10^{-4}$, step decay $\times 0.5$ at
        epochs 10, 20, 25
  \item Epochs: 30
  \item Gradient clipping: norm $\leq 1.0$
  \item Random seeds for replicated runs: 101, 202, 303 (plus 404, 505 for headline cells)
\end{itemize}

\subsection{Dataset Construction}
\label{app:dataset}
Waymo agent future trajectories are stored in \texttt{processed\_scenarios/}
PKL files with shape \texttt{(num\_agents, 91, 10)}. SMPL pedestrian files are
matched to Waymo object IDs via integer stem matching. 6{,}688 of 8{,}282
validation pedestrians (80.8\%) have a valid future-trajectory match and are
evaluated; the remainder are excluded. The 91-step grid spans $[0, 9.0]$
seconds; SMPL timestamps (originally in microseconds) are divided by $10^6$
before alignment. In the 10\,fps condition the SMPL observations cover only
$[0, 1.0]$\,s (the past window): across the entire training split only 5 of
91{,}520 future pose-target steps are nonzero, which is the basis for the
inert-geodesic-gradient analysis in Section~\ref{sec:loss_ablation}.

\subsection{Loss Weight Stability}
At $w=1.0$, gradient explosion through the SMPL forward kinematics causes NaN
divergence at epoch 3. The geodesic loss gradient norm was also found to diverge
at the $\pm 1$ boundary of the $\arccos$ argument; we clamp to $[-1+\epsilon, 1-\epsilon]$
with $\epsilon=10^{-7}$. At $w=0.1$, training is stable for all 30 epochs across
all ablation variants.

\subsection{Auxiliary Loss Weight}
\label{sec:weight_ablation}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{fig_ablation.pdf}
\caption{%
  \textbf{Loss ablation and WTA-L1 weight sensitivity.}
  \emph{Left}: \minADE for the seed-replicated supervision configurations
  (Table~\ref{tab:loss_ablation}); markers show individual seeds.
  \emph{Right}: single-seed sensitivity to the auxiliary WTA-L1 weight
  ($w \in \{0.05, 0.1, 0.2\}$), with the seed-noise scale measured in
  Section~\ref{sec:loss_ablation} shown as a shaded band. Within that noise,
  the apparent optimum at $w{=}0.1$ is weak evidence at best.
}
\label{fig:ablation}
\end{figure}

The earlier draft swept what it believed was the geodesic weight and found a
``sharp optimum'' at $w{=}0.1$: $w{=}0.05$ gave $0.6786$ and $w{=}0.2$ gave
$0.6660$, both single runs. Two corrections apply. First, because the geodesic
term is inert, the sweep actually varied the \emph{WTA-L1} weight (both weights
were scaled together in those configurations). Second, the differences involved
($0.02$--$0.06$) overlap the seed-noise scale measured in
Section~\ref{sec:loss_ablation}, so the curvature of this one-seed sweep should
not be read as a calibrated sensitivity estimate. What survives: $w{=}0.1$ is a
reasonable default, and we did not find evidence that performance is improved by
halving or doubling it.


\subsection{Ablation: Future Pose Supervision Quality}
\label{sec:future_ablation}

To localize the source of improvement, we compare two data conditions, both with
the full auxiliary loss set at $w{=}0.1$ (single runs):
(a) \emph{no future poses}: future pose GT is zero, only past poses are observed
(the same data condition as our other ablations); and (b) \emph{30\,fps
motion-prior-completed future poses}: the Waymo-3DSkelMo pipeline's
motion-prior infilling provides 99\% coverage of future timesteps as pseudo-GT.
Condition (b) is the one setting in which the geodesic and MPJPE terms receive
real (non-degenerate) targets and gradients.

The result: the 30\,fps model achieves \minADE $=0.6567$ and the zero-future model
$0.6532$---a difference of $0.0035$ (0.5\%), well within the seed noise measured in
Section~\ref{sec:loss_ablation}. \textbf{Supervising the pose decoder with real
future poses does not improve trajectory prediction}: even when the rotation-space
losses are given genuine gradients, trajectory accuracy does not move. Whatever
benefit pose conditioning provides must come from the past GRU encoder, not from
the auxiliary future-pose prediction task.


\subsection{Training Dynamics}

\begin{figure}[t]
\centering
\includegraphics[width=0.65\linewidth]{fig_training_curves.pdf}
\caption{%
  \textbf{Training dynamics.}
  Validation \minADE over training for the temporal encoding variants; lines are
  means over seeds, bands the per-seed range. Epoch-to-epoch and seed-to-seed
  variability is of the same order as the differences between several variants,
  which is why all comparisons in this paper are seed-replicated.
}
\label{fig:training_curves}
\end{figure}

Figure~\ref{fig:training_curves} shows validation \minADE over 30 training epochs
for the temporal encoding variants, with per-seed bands. Two qualitative
observations are stable across seeds: cross-attention without positional encoding
tracks the trajectory-only baseline throughout, and the pose-conditioned variants
exhibit larger epoch-to-epoch variability than the baseline. The GRU band is
dominated by its failed seeds, whose validation curves plateau near $0.72$--$0.83$
from early in training rather than diverging late---the failure is a bad basin,
not a late-training collapse---while the cross-attention+PE band is the
narrowest of all variants.


\subsection{Qualitative Examples}
\label{app:qualitative}

\begin{figure}[htbp]
\centering
\includegraphics[width=\linewidth]{fig_qualitative.pdf}
\caption{%
  \textbf{Qualitative comparison at full-Waymo-pretrained scale.}
  Four validation scenes. Past in gray; GT future dashed black with $\star$ endpoint. All 6
  predicted modes faded; top-scoring mode highlighted (red: no-pose; blue: pose).
  Per-scene \minADE in titles. Scenes selected from the largest per-pedestrian
  improvements of the pose model, so these illustrate the failure modes pose can fix,
  not typical magnitudes.
  \emph{Long-distance:} no-pose stops at 5m; pose follows the 25m walk.
  \emph{Curved:} no-pose underestimates displacement; pose follows the curve.
  \emph{Mode coverage:} both top modes underestimate, but pose has a faded mode aligned
  with GT.
  \emph{Stationary:} no-pose wrongly predicts motion; pose correctly stays near origin.
}
\label{fig:qualitative}
\end{figure}

\subsection{Original Single-Run Results (Earlier Draft)}
Table~\ref{tab:full} reports all 19 single-seed runs of the earlier draft, with
loss labels corrected per the audit: ``geo'' terms were inert (zero gradient,
Section~\ref{sec:loss_ablation}), so the functional supervision is what remains
after removing them. Runs 009 and 010 are functionally identical; their spread is
seed noise. These runs use unrecorded seeds and are superseded by the
seed-replicated matrix of Table~\ref{tab:multiseed}.

\begin{table}[h]
\caption{Earlier-draft single runs (corrected labels). ``Functional losses''
removes inert terms; $w$ in parentheses. Baseline for $\Delta$ is run 004.
Runs 001--002 are invalid (zero future trajectory GT).}
\label{tab:full}
\begin{center}
\small
\begin{tabular}{clllcc}
\toprule
Run & Draft tag & Encoder & Functional losses / Map / Pretrain & \minADE & $\Delta$ \\
\midrule
001 & full pose    & GRU & all (1.0) & 0.6114 & \textit{invalid} \\
002 & no pose      & --- & traj only & 0.7724 & \textit{invalid} \\
\midrule
003 & full     & GRU & mpjpe+wta+cls (0.1)           & 0.6532 & $-$3.2\% \\
004 & baseline & --- & traj only            & 0.6745 & --- \\
005 & full w=0.05 & GRU & mpjpe+wta+cls (0.05)           & 0.6557 & $-$2.8\% \\
006 & full w=0.2  & GRU & mpjpe+wta+cls (0.2)            & 0.6542 & $-$3.0\% \\
007 & 30fps    & GRU & mpjpe+geo+wta+cls (0.1), real future & 0.6567 & $-$2.6\% \\
008 & ``mpjpe only''   & GRU & mpjpe+wta (0.1)     & 0.6576 & $-$2.5\% \\
009 & ``wta only''     & GRU & wta (0.1)            & 0.6467 & $-$4.1\% \\
010 & ``geo only''     & GRU & wta (0.1) [geo inert]        & 0.6231 & $-$7.6\% \\
011 & ``geo w=0.05''   & GRU & wta (0.05) [geo inert]       & 0.6786 & $+$0.6\% \\
012 & ``geo w=0.2''    & GRU & wta (0.2) [geo inert]        & 0.6660 & $-$1.3\% \\
013 & cross-attn   & XAttn & wta (0.1) [geo inert]      & 0.6765 & $+$0.3\% \\
014 & xattn+PE     & XAttn+PE & wta (0.1) [geo inert]   & 0.6337 & $-$6.0\% \\
\midrule
015 & no-pose+map  & --- & traj only + map      & 0.4880 & $-$27.7\% \\
016 & pose+map     & GRU & wta (0.1) + map [geo inert]  & 0.4797 & $-$28.9\% \\
\midrule
017 & pretrain     & --- & traj only + map (full Waymo, 231k peds) & --- & \textit{ckpt only} \\
018 & finetune pose & GRU & wta (0.1) + map, pretrained & 0.3749 & $-$44.4\% \\
019 & finetune no-pose & --- & traj + map, pretrained & 0.3861 & $-$42.8\% \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\subsection{Seed-Replicated Matrix}
Table~\ref{tab:multiseed} lists every run of the replication matrix
(seeds 101/202/303, plus 404/505 for the headline, encoder, map, and pretrain
cells). The four audit cells were additionally re-run with the future-pose mask fix
(Section~\ref{sec:loss_ablation}); those masked runs are in the code release.

\begin{table}[h]
\caption{Seed-replicated matrix: best-checkpoint \minADE per seed (seeds
101/202/303, plus 404/505 for headline cells). All runs 30 epochs, identical
protocol; ``geo (inert)'' marks the zero-gradient geodesic term carried by some
configurations.}
\label{tab:multiseed}
\begin{center}
\footnotesize
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{llcc}
\toprule
Cell & Configuration & \minADE mean$\pm$std & per-seed values \\
\midrule
baseline   & no pose                          & $0.6604 \pm 0.0143$ & .6550 .6831 .6641 .6541 .6456 \\
geo\_pure  & GRU, no active aux (geo inert)   & $0.6545 \pm 0.0271$ & .6958 .6650 .6392 .6259 .6466 \\
wta01      & GRU, WTA-L1 + geo (inert)        & $0.6853 \pm 0.0651$ & .6390 .7848 .6292 .7167 .6570 \\
gmm\_only  & GRU, WTA-L1 (same config as wta01) & $0.7403 \pm 0.1354$ & .8967 .6621 .6621 \\
mpjpe      & GRU, MPJPE + WTA-L1              & $0.6769 \pm 0.0421$ & .7243 .6622 .6441 \\
full       & GRU, MPJPE+WTA+cls (geo inert) & $0.6574 \pm 0.0237$ & .6680 .6740 .6302 \\
xattn      & XAttn no PE, WTA-L1 (+ geo inert) & $0.6667 \pm 0.0407$ & .7136 .6408 .6457 \\
xattn\_pe  & XAttn+PE, WTA-L1 (geo inert) & $0.6549 \pm 0.0278$ & .6395 .6343 .6374 .6631 .7002 \\
norootorient & XAttn+PE, root-orient zeroed & $0.6633 \pm 0.0031$ & .6627 .6666 .6605 \\
\midrule
map\_nopose & no pose, with map               & $0.4854 \pm 0.0047$ & .4903 .4839 .4886 .4783 .4860 \\
map\_wta01  & GRU, WTA-L1, with map           & $0.4854 \pm 0.0057$ & .4798 .4911 .4853 \\
map\_xattn\_pe & XAttn+PE, WTA-L1, with map    & $0.4778 \pm 0.0041$ & .4769 .4811 .4795 .4710 .4804 \\
\midrule
ft\_nopose  & no pose, map, pretrained        & $0.3745 \pm 0.0071$ & .3815 .3753 .3804 .3706 .3645 \\
ft\_wta01   & GRU, WTA-L1, map, pretrained    & $0.3718 \pm 0.0059$ & .3744 .3760 .3651 \\
ft\_xattn\_pe & XAttn+PE, WTA-L1, map, pretrained & $0.3718 \pm 0.0034$ & .3710 .3712 .3777 .3694 .3697 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\end{document}
